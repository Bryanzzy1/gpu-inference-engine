# Normalize line endings to LF in the repo, regardless of the OS committing.
# Stops the "LF will be replaced by CRLF" churn on Windows.
* text=auto eol=lf

# Binary data that must not be normalized.
*.png binary
*.zip binary
*.bin binary
